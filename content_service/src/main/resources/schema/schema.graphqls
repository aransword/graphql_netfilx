type Query {
    # ID 기반으로 컨텐츠 조회
    show(id: ID!): Show
    # 전체 컨텐츠 리스트 조회
    shows: [Show]
}

# @key 지시어를 통해 id가 이 타입의 고유 식별자임을 라우터에게 알립니다.
type Show @key(fields: "id") {
    id: ID!
    title: String!
    description: String
    releaseYear: Int
    previewVideoUrl: String
}