type Query {
    # 특정 유저 1명 조회
    user(id: ID!): User
    # 전체 유저 목록 조회 (테스트용)
    users: [User]
}

# @key 지시어를 통해 id가 이 타입의 고유 식별자임을 라우터에게 알립니다.
type User @key(fields: "id") {
    id: ID!
    username: String!
    name: String!
    email: String!
}